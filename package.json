{
	"name": "godrej-conscious-collective",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"description": "An event listing website and the Strapi CMS behind it.",
	"license": "ISC",
	"author": "",
	"keywords": [],
	"engines": {
		"node": ">=22.22.0",
		"pnpm": ">=10.29.0"
	},
	"scripts": {
		"dev": "node scripts/ensure-local-env.js && pnpm --recursive --parallel run dev",
		"build": "pnpm --recursive run build",
		"start": "pnpm --recursive --parallel run start",
		"typecheck": "pnpm --recursive run typecheck",
		"format": "dprint fmt",
		"format:check": "dprint check",
		"test": "node scripts/ensure-local-env.js && pnpm --recursive run test"
	}
}
