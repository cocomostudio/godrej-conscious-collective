
/**
 |
 | Border widths.
 |
 | Adds the hairline `0.5px` rule used for low-contrast dividers, so markup can
 | say `border-b-0.5` instead of `border-b-[0.5px]`. Merged via `extend`, so
 | Tailwind's stock scale (DEFAULT / 0 / 2 / 4 / 8) remains.
 |
 */

export const border_width = {
	"0.5": "0.5px",
}
