
/**
 |
 | Populate fragment for `list.session-listing-with-filtration-v1`.
 |
 | A category and nothing else, so there is nothing to populate. It is named
 | anyway, because the section list needs an entry for every component the zone
 | admits and a component missing from that map arrives with no attributes at
 | all.
 |
 | The rows are spliced in afterwards, by `src/this/api/listings.ts`, exactly as
 | the home page's session listing has its rows spliced in. What differs is the
 | cap: there is none here. See the note beside the resolver.
 |
 */

export const populate_session_listing_with_filtration_v1 = {}
