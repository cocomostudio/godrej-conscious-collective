
/**
 |
 | Populate fragment for `list.session-listing-v1`.
 |
 | A session listing holds no relation at all — a category and a count are the
 | whole of it — so there is nothing to populate. It is named anyway, because
 | the section list needs an entry for every component the zone admits and a
 | component missing from that map arrives with no attributes at all.
 |
 */

export const populate_session_listing_v1 = {}
