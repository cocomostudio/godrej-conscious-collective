
/**
 |
 | Populate fragment for `navigation.link-v1`.
 |
 | A leaf: label, url, style and text_color are all scalars, so there is nothing
 | to reach for. The empty map is deliberate rather than missing — a component named in
 | a dynamic zone's `on` map with no populate of its own still arrives with its
 | own attributes.
 |
 */

export const populate_link_v1 = {}
