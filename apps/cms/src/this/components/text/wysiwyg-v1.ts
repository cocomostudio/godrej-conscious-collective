
/**
 |
 | Populate fragment for `text.wysiwyg-v1`.
 |
 | A leaf. Its one attribute is a `blocks` field, which arrives whole as a
 | column value — rich text is not a relation and there is nothing to reach for.
 |
 */

export const populate_wysiwyg_v1 = {}
