
/**
 |
 | Populate fragment for `text.wysiwyg-v1`.
 |
 | A leaf. Its `rich_text` is a `blocks` field, which arrives whole as a column
 | value — rich text is not a relation and there is nothing to reach for — and
 | its `text_color` is a scalar beside it.
 |
 */

export const populate_wysiwyg_v1 = {}
