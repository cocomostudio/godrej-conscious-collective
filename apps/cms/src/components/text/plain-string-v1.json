{
	"collectionName": "components_text_plain_string_v1",
	"info": {
		"displayName": "Plain string",
		"description": "A single line of unformatted text."
	},
	"options": {},
	"attributes": {
		"content": {
			"type": "string"
		},
		"text_color": {
			"type": "enumeration",
			"enum": [
				"black",
				"white",
				"context"
			],
			"default": "black",
			"required": true
		}
	},

	"__": {
		"note": "Admin metadata for this schema. Strapi ignores this key; src/this/admin-metadata reads it and writes it into the content manager's stored configuration on every boot.",
		"metadatas": {
			"content": {
				"edit": {
					"label": "Content",
					"description": "Plain text. No formatting is applied and none is available."
				},
				"list": {
					"label": "Content"
				}
			},
			"text_color": {
				"edit": {
					"label": "Text colour",
					"description": "The colour of the words. \"context\" is the page’s own colour, which follows the event it belongs to."
				},
				"list": {
					"label": "Text colour"
				}
			}
		},
		"layouts": {
			"edit": [
				[
					{ "name": "content", "size": 12 }
				],
				[
					{ "name": "text_color", "size": 6 }
				]
			]
		}
	}
}
